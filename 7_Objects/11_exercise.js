// destructure the key "first-name" as a variable called firstname

const user = {
    "first-name" : "Harsh",
};

// fetching property/Key named first-name from user and storing it value in variable firstname
let {"first-name":firstname} = user;