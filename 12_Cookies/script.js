// Cookies : 4kb, Can expire Automaticallly or when the he browser closes (session cookies), JavaScript (unless HttpOnly) and the server, Authentication, session management, user tracking

// local Storage : 5-10mb, persist until removed,JavaScript (localStorage), User preferences, theme, cart, app data